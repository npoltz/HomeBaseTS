<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <% if (locals.title) { %>
        <title>Home Base | <%= title %></title>
    <% } else { %> 
        <title>Home Base</title>
    <% } %> 
</head>