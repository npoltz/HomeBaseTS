version: '3.9'

services:
#   homebase.api:
#     image: ${DOCKER_REGISTRY-}homebasetsapi
#     build:
#       context: .
#       dockerfile: HomeBaseTs.Api/Dockerfile
#     network_mode: bridge
#     ports:
#         - "80"
#     links:
#         - "db"
  db:
    image: mongo
    volumes:
        - db-data:/data/db
    container_name: "mongo-01"
    network_mode: bridge
    ports:
        - "27017:27017"

volumes:
   db-data:
    external: true